import Logo from "@/components/Logo";
import React from "react";

function Loading() {
  return (
    <div className="flex items-center justify-center w-full h-[1024px]">
      <Logo w={50} h={50} />
    </div>
  );
}

export default Loading;
